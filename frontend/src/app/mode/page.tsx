"use client";
import { CustomLayout } from "@/layout";
import { ModeScreen } from "@/templates";

export default function Mode() {
  return (
    <CustomLayout>
      <ModeScreen />
    </CustomLayout>
  );
}
